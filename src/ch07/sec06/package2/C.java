package ch07.sec06.package2;

public class C {

    public void method() {

        /* protected 키워드를 사용해서 다른 패키지에서는 접근불가 상속을 통해서만 접근 가능

        A a = new A();
        a.field = "value";
        a.method();

        */
    }
}
